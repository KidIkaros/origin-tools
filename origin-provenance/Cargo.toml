[package]
name = "origin-provenance"
# Origin-provenance carries its own version (0.4.5) — no SDK version change
# (P-05 acceptance: version bump here only).
version = "0.4.5"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "File provenance — stamps, watermarks, directory scan, verification"

[dependencies]
origin-common = { path = "../origin-common" }
origin-crypto-sdk.workspace = true
origin-attest = { path = "../origin-attest" }
origin-proof = { path = "../origin-proof" }
clap.workspace = true
serde.workspace = true
serde_json.workspace = true
hex.workspace = true
base64 = "0.22"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
